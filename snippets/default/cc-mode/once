#name : #ifndef XXX; #define XXX; #endif
# --
#ifndef ${1:_`(apply 'string (mapcar (lambda(c) (or (and (= c ?-) ?_) c)) (string-to-list (upcase (file-name-nondirectory (file-name-sans-extension (buffer-file-name)))))))`_H_}
#define $1

$0

#endif /* $1 */